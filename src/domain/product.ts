/**
 * Domain entity placeholder — proves the `@/domain/*` import path works.
 *
 * NOTE: This is intentionally minimal. The real Product/Category model is
 * defined in Step 2 (Data Modeling). Do not build on this shape yet.
 */
export interface Product {
  id: string;
  name: string;
}
